Subcategories:

- new Cell

X new Model

X mod api: topics: only with parent = null;
X new api: topicsWithParentTopic:();

X new mutableSet with open/closed categories
X fetch logic

X two new method to build topic lists:
X (NSArray *)buildTopics (check for deleted topics)
	(invoked viewWIllAppear)
	(reloadForNewContents)

- willSelectCellAtIndexpath
	X skip selection for parent categories
	X trigger insertion/deletion of new cells:
	X add/remove topic id to set
	- bug: on select/deselect handle ivar _previuousSelection
	- bug: on pulltorefresh crashes

- didSelectCell

- cellForRowAtIndexPath:
- check for category/subcategory, return appropriate cell


