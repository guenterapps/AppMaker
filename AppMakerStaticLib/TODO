X set hidden flag
X fix app flow
X logic to handle 5 minutes cache
X handled http errror
X remove phone and number cells
X set a limit to the new location (mantaints the last one) -- REMOVED
- add attribs columns
X pull to refresh
x handle etag
X async load immagini --> avoid multiple times, avoid infinite loop, load on detail view
X pulizia cache
X add order property on every object
X zoom efficiently map
X facebook and twitter
X select first topic on load (if any)
- fix map flickering
X Credits view
X getUi flow
X migrate from image property to image link
X YOUTUBE
- PUSH notification
X handle set topic on mapview!


-- Release NOTE

- tasti chiama/web
- apertura singolo elemento
- navigazione in app

-- NB: cancellare l'app prima di installare!



